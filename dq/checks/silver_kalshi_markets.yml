checks for silver_kalshi_markets:
  # Kalshi partitions may be empty when the sample/live CLI path did not materialize Kalshi.
  - missing_count(fetched_at) = 0
  - missing_count(market_ticker) = 0
  - duplicate_count(fetched_at, market_ticker) = 0
